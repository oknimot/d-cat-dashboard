import React from "react";

import { TableWidgetProps } from "../../core/types/widget.types";

const TableWidget: React.FC<{ widget: TableWidgetProps }> = () => {
  return <div>TableWidget</div>;
};

export default TableWidget;
