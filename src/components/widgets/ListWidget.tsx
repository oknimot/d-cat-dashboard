import React from "react";

import { ListWidgetProps } from "../../core/types/widget.types";

const ListWidget: React.FC<{ widget: ListWidgetProps }> = () => {
  return <div>ListWidget</div>;
};

export default ListWidget;
