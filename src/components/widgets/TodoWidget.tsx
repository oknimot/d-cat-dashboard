import React from "react";

import type { TodoWidgetProps } from "../../core/types/widget.types";

const TodoWidget: React.FC<{ widget: TodoWidgetProps }> = () => {
  return <div>TodoWidget</div>;
};

export default TodoWidget;
